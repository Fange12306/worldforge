# WorldForge 历史推演模式 — 完整设计框架

> 2026-08-10 讨论定稿。新增"历史推演"顶级模式：给定世界初始状态与世界法则，由多个实体子 agent 并行推演宏观历史。涌现式实体增灭、严格自洽、可参数化随机性/严肃性。

---

## 一、定位与核心目标

### 一句话定义

**历史推演 = 世界自己写历史。** 输入一个架空世界（初始状态 + 世界法则 + 各地区初始形态），输出一条自洽的宏观历史演化轨迹——地区的分裂、吞并、灭亡、崛起不是预设剧情，而是涌现结果。

### 与现有功能的关系（重要）

现有功能是"编辑器"——人写世界。历史推演是"播放器"——世界自己动。两者**并行共存，互不干扰**。

| | 编辑器（现有） | 播放器（历史推演，🆕） |
|---|---|---|
| 状态来源 | 用户/AI agent 编辑 | 推演引擎自动推进 |
| 数据 | `entries/` `timelines/` `relations/` | `simulation/`（独立，默认不回写） |
| 交互 | 手动 | 播放/暂停/步进 |
| 一致性 | consistency check（写前校验） | 片尾仲裁（对账合并） |
| 时间 | 用户掌控 | tick 驱动（可变粒度） |

**硬性要求：不破坏现有功能。** 推演引擎是并行子系统，全程不注册改动现有 ~60 个 IPC 命令，不侵入 chat/entry/timeline 面板。

### 核心设计原则

1. **宏观优先**——推演的是"国家与文明"，不是村庄日常。实体状态用紧凑结构化卡片，事件只记录结构性变化。
2. **自主涌现**——地区/实体数量无上限、无预设，由世界演化决定。
3. **自洽是硬约束**——任何时刻的世界状态不矛盾。自洽靠三条防线：世界法则（约束）、片尾仲裁（对账）、确定性 ID（防重复）。
4. **可参数化**——随机性（小概率事件发生率）、严肃性/因果严密性（对巧合的容忍度）、粒度（宏观/常规/微观）。
5. **成本随历史走**——注意力分层，token 消耗跟随历史自身的动荡程度。

---

## 二、隔离策略（新模式）

### 数据隔离

```
<world>/
├── entries/          ← 现有（不被推演直接写入）
├── timelines/        ← 现有
├── relations/        ← 现有
└── simulation/       ← 🆕 历史推演子系统（完全独立）
    ├── config.json          推演参数（随机性/严肃性/粒度/预算）
    ├── session.json         当前推演会话状态（当前 tick、活跃 agent 数）
    ├── laws.json            世界法则全集（恒定底座, §4.0①）
    ├── space.json           空间全景（按世界真实尺度铺全, §4.0②）
    ├── registry.json        维度注册表（动态涌现, §3.5）
    ├── lore.json            背景规则库（细化即锁定, §3.6）
    ├── decree.json          干预指令（用户外部干预, §3.7）
    ├── entities/
    │   └── <entity_id>.json 实体卡片（一个活跃实体一个文件）
    ├── events/
    │   └── <tick>.json      每 tick 的推演事件（全局日志，时序有序）
    ├── archive/
    │   └── <entity_id>.json 灭亡/消失实体的历史档案
    ├── branches/            反事实分叉存档（what-if）
    └── MEMORY.md            （可选）推演摘要索引
```

### 代码隔离

- 前端：`src/lib/simulation/`（调度器、agent 通信、仲裁器）+ `src/components/simulation/`（UI）。
- 后端：`src-tauri/src/commands/simulation.rs` + `src-tauri/src/services/simulation/`，**独立模块**，不触碰现有 `commands/mod.rs` 的注册区（或仅在最末尾追加一个 `simulation` 命令组）。

### 桥接（可选，默认关）

推演产物与正式世界之间只有一条**用户手动触发的桥**："提交到正式世界"——把选定的推演事件批量导入 `timelines/`，自动触发现有的 `event_cascade`（关系图、词条 `timeline_summary` 全部自动更新）。默认不自动提交，因为推演是探索性的，多数分支不该污染正式设定。

---

## 三、数据模型

### 3.1 实体卡片（Entity Card）

**推演的最小决策单位是"实体"（entity），不一定是政体。** 一个实体 = 一个具备自主决策能力的单位。它可以是一个独立文明/国度，也可以是非国度的部落/部族/流浪民族/海外殖民地，甚至可以是同一种族内部的一个分支（人类有不同国度、文化、信仰）。

关键区分（正交的两个维度）：

- **种族（species/ethnic）** = 身份属性（精灵、兽人、人类…）
- **政权形态（political_form）** = 政治组织形态（部落/城邦/王国/帝国/无政权…）

一个实体 = 种族 × 政权形态 的任意组合。精灵可以不是国却是独立决策实体；人类可以因国度分裂成多个实体。

**"政体"只是实体的一种常见形态，不是全部。** 一个活跃实体一个文件。**结构化为 JSON，不做长散文**——这是宏观粒度与 token 成本的关键。

```typescript
type EntityCard = {
  id: string;                    // UUID — 稳定标识
  name: string;                  // "东境诸国" / "森林精灵部族" / "流浪民"
  parent?: string;               // 分裂/衍生前的父实体 id（用于追溯）
  kind: "entity" | "micro";      // entity=独立推演, micro=长尾批量聚合
  status: "active" | "dormant" | "extinct";

  // ── 核心指标（数值层, 确定性底座）──
  // 由物理层数值引擎维护（§4.1）。具体有哪些指标由世界法则派生——
  // 真实世界有 population/food/military；魔法世界可能换成 魔力/修炼进度/法阵覆盖。
  // 此处列的是"真实世界/类地球世界"的常见最小集，不是通用固定值。
  metrics: {
    population: number;          // 人口
    food: number;                // 粮食盈余/赤字（季节性）
    military: number;            // 军力
    legitimacy: number;          // 统治合法性（无政权形态时表示"内部凝聚力"）
    stability: number;           // 内部稳定度
  };

  // ── 维度值（发展轴, 动态涌现）──
  // tech/values 不是写死的字段，而是"维度注册表"（§4.2）中的已注册维度在此实体的取值。
  // 每个注册维度 0-100。潜力上限由世界法则/空间（§4.0）决定；实际值 ≤ 潜力上限。
  // 注册表初始为空或最小集，随推演涌现（如"航海""真气修为""对力量之源的敬畏"）。
  tech: Record<string, number>;        // 技术类已注册维度 → 当前值
  values: Record<string, number>;      // 理念类已注册维度 → 当前值

  // ── 身份与形态（叙事层）──
  identity: {
    species: string;             // 种族: 精灵/兽人/人类... （身份属性）
    ethnicity: string;           // 民族/文化分支（同一物种内部分化）
    culture: string;             // 文化
    political_form: string;      // 政权形态: 部落/城邦/王国/帝国/无政权/游牧联盟...
    ideology: string;            // 主导意识形态
    religion?: string;           // 宗教/信仰体系
    origin_story: string;        // 起源叙事（供后代 agent 引用）
  };

  // ── 位置与邻接（空间层）──
  geography: {
    region: string;              // 所在区域（生物群系/地形, 来自空间全景 §4.0②）
    neighbors: string[];         // 相邻实体 id（基于空间全景的邻接）
    capital?: string;            // 都城/核心聚落（无政权形态时可为定居点/营地）
  };

  // ── 对外关系（Board, 对外）──
  relations: {
    target: string;              // 实体 id
    stance: "war" | "tension" | "neutral" | "alliance" | "vassal" | "absorbed" | "rival";
    note?: string;               // 一句话状态说明
  }[];

  // ── 内部状态（Stick, 对内）──
  internal: {
    recent_events: string[];     // 最近 3-5 个内部事件摘要（供下一 tick 引用）
    active_issues: string[];     // 未解决议题（继承/未决危机）
    succession?: string;         // 掌权者/继任线索
  };

  // ── 元信息 ──
  active_level: "hotspot" | "regular" | "longtail";  // 注意力分层结果
  last_tick: number;             // 最后活跃 tick
  created_at: number;
  updated_at: number;
};
```

**维度说明（动态涌现，决定差异性）：**

- `tech`/`values` 不是固定字段，而是**维度注册表**（§4.2）中已注册维度在此实体的取值，用 `Record<string, number>` 表达（维度名 → 当前值）。维度名由世界法则/推演涌现，不由我们预设。
- 每个已注册维度有**潜力上限**（由世界法则/空间 §4.0 决定）和**当前值**（实际发展）。
- 实际值到潜力上限的转化由**内部投入**（legitimacy/stability）和**文化适配**（values）共同决定。
- **机制示例**（不是维度清单）：一个实体可以"技术够但理念不转"——若"航海"维度潜力高、实际值也高，但"探索欲"理念低 → 不产生探索行动。这正是"大航海"案例（欧洲技术+理念→探索；中国技术够但理念/政局→不走）的机制化——**但"航海"只是这个机制的一个实例，魔法世界可能涌现"修为"维度、"对力量之源的敬畏"理念**。具体涌现哪些维度，由世界法则（§4.0）与推演（§4.2）共同决定。

### 3.2 推演事件（Simulation Event）

每个 tick 一张 `events/<tick>.json`。**只记录结构性变化**，宏观粒度。

```typescript
type SimulationEvent = {
  id: string;
  tick: number;
  time_label: string;            // 人类可读时间, 如 "纪年 327 年"
  type: "war" | "founding" | "collapse" | "secession" | "conquest"
      | "migration" | "tech" | "reform" | "disaster" | "diplomacy"
      | "cultural" | "religion" | "other";
  participants: string[];        // 涉及实体 id（含旁观者? 不, 仅直接参与）
  region: string;
  description: string;           // 2-3 句叙事
  changes: {                     // 结构化后果（供仲裁器应用）
    entity: string;
    metrics?: Partial<EntityCard["metrics"]>;
    tech?: Partial<EntityCard["tech"]>;
    values?: Partial<EntityCard["values"]>;
    stance?: string;
    absorbed_by?: string;
    founded?: { name: string; from: string; ... };
  }[];
  causals?: string[];            // 前因事件 id（因果链追踪）
  random: boolean;               // 是否黑天鹅事件
};
```

### 3.3 世界模型（Laws + Space）

世界模型是推演的恒定底座，由四部分组成（见 §四）：世界法则全集（§4.0①）、空间全景（§4.0②）、物理层数值引擎（§4.1）、动态维度注册表（§4.2）。从确定性到叙事性，见第四节。

### 3.4 推演参数（config.json）

```typescript
type SimulationConfig = {
  randomness: number;            // 0-1 黑天鹅事件发生率（乘数）
  surprise: number;              // 0-1 意外结果比例（计划外结局）
  rigor: number;                 // 0-1 因果严密性（对巧合的容忍度, 高=拒绝巧合救场）
  granularity: "macro" | "standard" | "micro";  // 推演粒度（默认 macro）
  tickDuration: "year" | "decade" | "era";      // 时间片基准时长
  autoJump: boolean;             // 时代跳跃（平顺期批量推进）
  maxTicks: number;              // 本次推演总 tick 上限
  budget: {
    perTickGlobal: number;       // 每 tick 全局 token 上限
    perEntity: number;           // 每实体每次推演 token 上限
    hotspotMultiplier: number;   // 热点地区倍率
  };
  infoDelay: number;             // 信息传播延迟 tick 数
  maxEntities: number | null;    // 实体数量上限（null = 无上限, 涌现决定）
};
```

### 3.5 维度注册表（Dimension Registry）

维度注册表是**推演中涌现的、受数值引擎追踪的发展轴清单**。初始为空或最小集，随推演动态增删（见 §4.2）。每个实体卡片通过 `tech`/`values` 的 `Record<string, number>` 引用这里注册的维度名。

```typescript
type DimensionRegistry = {
  // 每个注册维度：名称 → 定义
  dims: Record<string, DimensionDef>;
  // 注册表演化历史（供"时代"轨迹分析，见 §4.2）
  history: {
    tick: number;
    action: "register" | "promote" | "demote" | "retire";
    dim: string;
    reason: string;
  }[];
};

type DimensionDef = {
  name: string;                  // 维度名（中文, 如 "航海" / "真气修为" / "对力量之源的敬畏"）
  kind: "tech" | "value";        // tech=技术/能力类; value=理念/倾向类
  // 潜力上限来源：由世界法则/空间（§4.0）决定。真实世界由地理决定
  //（沿海→航海潜力高）；魔法世界由法则决定（灵气浓度→修炼潜力高）。
  potential: number;             // 0-100 潜力上限（法则/空间推导）
  // 该维度对结果的影响权重（由升格判定计算，见 §4.2）
  weight: number;
  description?: string;          // 一句话说明（供 agent 理解）
  first_tick: number;            // 首次注册 tick
  last_active: number;           // 最后活跃 tick
};
```

维度注册表存于 `simulation/registry.json`。

### 3.6 背景规则库（Canonical Lore）

背景规则库是**世界在空间与时间两个向度上"单向累积、永不回溯"的不可更改事实层**（见 §4.0 核心规则 2、3）。包含三类不可更改事实：

- **空间向（细化即锁定）**：初始全景（世界法则全集 + 空间全景）是顶层的背景规则；每次细化产生的确定细节追加进去，逐层加深。
- **时间向（历史即锁定）**：已发生的历史事实（"某事件确实发生""某实体在某 tick 吞并了 X"）——它们由唯一事实源（事件日志 + 实体卡片快照）锁定，是不可更改的过去。
- **时间向（细化过去）**：在已发生的时代向下细化产生的人物/事件/细节（用户干预 `direction: "past"`，或推演中补全过去的细节）——不改变已确定的宏观轨迹，且必须与已发生的未来兼容；一旦确定即锁定。
- 初始全景的确定事实（`source: "initial"`）。

所有 agent 的写入在仲裁时必须符合背景规则库，且**不能在已确定的层级上修改**——只能在更细层级新增（空间向 + 细化过去），或在其上叠加新历史（时间向宏观）。空间向的细化用 `layer` 标记深度；时间向的宏观历史 `layer` 为 0，细化过去的细节用 `layer` 标记其在该时间点的细化深度。

```typescript
type CanonicalLore = {
  // 逐层锁定的确定事实。每个条目是"一个已确定、不可回溯修改的事实"。
  facts: LoreFact[];
  // 层级数 = 细化深度（初始全景 = 层级 N，往下 N+1, N+2...）
  max_layer: number;
};

type LoreFact = {
  id: string;                    // UUID
  axis: "space" | "time";        // 空间向（细化即锁定）vs 时间向（历史锁定 + 细化过去）
  layer: number;                 // 细化层级（空间向: 大陆→区域→子区域; 时间向: 宏观=0, 细化过去的细节越深越大）
  scope: string;                 // 事实适用的空间/概念范围（如 "西环大陆/东南部"）或时间范围（如 "第三纪元"）
  content: string;               // 事实内容（"该子区域为山地, 铁矿丰富" 或 "实体 X 在 tick 37 吞并了 Y" 或 "第三纪元有一位预言者"）
  source: "initial" | "refinement" | "history" | "past_refinement";  // 初始全景 / 推演中细化 / 已发生历史 / 细化过去
  locked_tick: number;           // 锁定时的 tick（initial = 0）
  refined_from?: string;         // 若由某事实细化而来, 记录父事实 id
  notes?: string;                // 细化的合理性依据 / 事件的因果依据
};
```

背景规则库存于 `simulation/lore.json`。

**与维度注册表的区别**：维度注册表是**动态的**（维度可注册/消退），背景规则库是**单向累积的**（事实锁定后不可改）。两者都约束 agent 写入，但一个描述"追踪什么"，一个描述"已确定的事实"。

### 3.7 干预指令（Decree）

用户对推演的**外部干预**——可以面向未来（指定之后的某件事/走向），也可以面向过去（细化历史上的某个时代）。存于 `simulation/decree.json`。

```typescript
type Decree = {
  id: string;                    // UUID
  direction: "future" | "past";  // 面向未来(走向引导) vs 面向过去(历史细化)
  target_tick: number;           // 注入/细化的目标 tick（future = 从哪开始生效; past = 细化哪个时代）
  // 目标：作用于哪个实体 / 区域 / 或全局
  target: {
    type: "entity" | "region" | "global";
    id?: string;                 // 实体 id 或区域 id（global 时省略）
  };
  // 指令内容：
  //   future: 指定某件事发生 / 指定一个历史走向
  //   past:   在目标时代创造一个人物/事件/细节（细化历史）
  intent: string;                // future: "未来五十年, X 国将统一东境"
                                 // past:   "第三纪元曾有一位预言者, 他是后来那场变革的远因"
  strength: "command" | "lean" | "nudge";  // 命令 / 趋势引导 / 轻微倾向
  // 判定结果（由仲裁器判定后回填, §5.5）
  verdict?: "accepted" | "adjusted" | "twisted" | "rejected";
  verdict_note?: string;         // 判定说明（为什么调整/扭曲/拒绝）
  effective_tick?: number;       // 判定通过的 tick
};
```

**两种方向，同一套判定：**

- **面向未来（future）**：指定之后的走向。只影响 `target_tick` 及之后的历史，不能改写已发生的历史。
- **面向过去（past，历史细化）**：在已发生的时代**向下细化**——创造人物/事件/细节，填充过去（不改变已确定的宏观历史轨迹，只让过去更丰满）。这是"细化即锁定"在时间向度的体现（与空间向度的细化对称，§4.0 核心规则 2、3）。

**面向过去的额外约束（关键）：** 细化的过去**不能与已发生的未来矛盾**——如果在 tick 3 创造一个人物，他必须与 tick 4-10 已确定的事件兼容（否则就相当于改写了未来）。同样经过严格判定（历史法则 + 历史惯性 + 与已发生未来兼容，§5.5）。一旦细化确定即锁定，不可再改。

**判定结果谱系**（详见 §5.5）：完全生效 / 打折生效 / 扭曲生效 / 拒绝生效。

---

## 四、世界模型（全景初始化 + 动态维度）

世界模型是**自洽的底层保障**。所有 agent 写入先过这一关。

核心原则（承上启下）：

> **世界模型（法则 + 空间）恒定而全面，维度表示（发展轴）动态而涌现。**
> 世界法则/地图是唯一固定的输入，所有时代都运行在它上面——就像现实世界从大航海到全球化，无数不同性质的时代，全都跑在同一套物理学和其他法则下。法则没变过，变的是社会拿法则去做什么（涌现）。
>
> **"什么构成发展"是涌现的，不是预设的。** 1300 年没人知道"大航海"会成为时代主题。严肃宏观推演不能预先承诺任何时代主题，必须能无偏地跑过许多不同性质的纪元——维度随历史自行升起和消退。

### 4.0 全景初始化（最大原则）

初始化必须**尽可能全、尽可能准**，一次性把世界的全部基础铺满，之后推演就只在这套完整底座上跑，不需要中途补。消耗性能和时间无所谓——这是一次性成本，换的是后续推演自洽的确定性底座。**初始化越全越准，后续越不需要临时补丁，自洽性越强。**

**三条核心规则——世界在空间与时间两个向度上都"单向累积、永不回溯"：**

1. **初始是"粗但全面"的底座**——空间全景铺满真实尺度，但具体到"某子区域长什么样"可以是概略的。初始不需要细化到每个角落，只需要全面、不遗漏任何空间位置。
2. **细化即锁定（空间向，单向细化，永不回溯）**——推演中涉及某个初始时只概略描述的区域/细节时，在底座上**向下细化**它（根据合理性）。**一旦细化确定，即写入背景规则，成为不可更改的事实，后续不能再改**——除非继续往更细层级细化。绝不在已确定的层级上修改，只在更细的层级新增。
3. **历史即锁定（时间向，宏观轨迹锁定，允许向下细化过去）**——新的历史必须**建立在已发生的历史上**，由"合理性 + 随机性"推演（见下）。**事件一旦经仲裁确定，即写入唯一事实源（事件日志 + 实体卡片快照），成为不可更改的历史**——宏观历史轨迹锁定，不能推翻或改写。但如同空间向度允许细化，**时间向度也允许在已发生的时代向下细化**（用户/推演创造过去的人物、事件、细节，填充历史的更细层级），且**不能与已发生的未来矛盾**。细化的过去一旦确定即锁定。这是"细化即锁定"与"历史即锁定"在时间向度的统一。

```
空间向（细化即锁定）                时间向（历史即锁定 + 细化过去）
层级 N（初始全景）   大陆/星域       已发生历史（宏观事件日志+卡片快照）
     ↓ 涉及子区域, 细化             ↓ 新历史建立在已发生历史上（+ 合理性+随机性+干预）
层级 N+1            具体化·锁定     新事件 → 仲裁确定 → 宏观锁定
     ↓ 不再回溯                    ↓ 也可在已发生时代向下细化（创造人物/事件）
层级 N+2            ...           细化过去 → 判定 → 锁定（不能与已发生未来矛盾）
```

**历史推演总公式：**

```
新历史 = 已发生历史（唯一事实源） + 合理性（世界法则/物理层/因果） + 随机性（黑天鹅/意外） + 干预（用户指令, 过判定后生效）
```

四项全部被覆盖：**已发生历史**由唯一事实源保证（§5.3），**合理性**由物理层数值引擎 + 规则层硬约束 + 片尾仲裁保证（§4.1/§4.3/§5.3），**随机性**由黑天鹅发生器和 surprise 意外结果保证（§7），**干预**由用户注入的 Decree（§3.7）经历史法则与历史惯性判定后生效（§5.5）。推演不是凭空编故事，而是严格在"已发生的确定性底座"上，用合理性与随机性向前推进，并允许用户在法则内注入历史走向。

背景规则库（§3.6）= 累积的"不可更改"事实层。初始全景是顶层的背景规则；每次细化产生的确定细节、以及历史上确立的确定事实（如"某事件确实发生""某实体确实在 tick 37 吞并了 X"）都追加进去，逐层加深。

初始化由三大块组成：

#### ① 世界法则全集（时间向的"规律全集"）

凡这个世界成立的一切规律，初始化时尽量穷举，作为**恒定不动**的底层。漏一条法则，推演就可能在某处偷偷违反它而不自知。

- **本体规则**：空间规律（宇宙尺度）、守恒律（能量/质量/……）、时间、因果的运作方式。
- **世界的"第二物理"**：魔法/真气/灵气/灵力等——它们在现实物理之外，是这个世界自带的额外本体规则。这个世界的"可能性空间"由这些法则决定，而不是真实世界的物理。
- **边界条件**：这个世界哪里是尽头（可观测宇宙？星球？大陆？），哪里不可逾越。

> 现实世界的整个法则集（物理学等）是"这个世界遵循宇宙空间规律"时自动带入的——它定义了这个世界的可能性空间。魔法/真气世界则在现实物理之上叠加额外法则，可能性空间随之改变。

#### ② 空间全景（空间向的"世界全景"）

按世界的**真实尺度**铺满，不阉割、不"够故事用就行"：

- 如果这个世界遵循宇宙空间规律，初始化范围就是**整个可观测宇宙**（天体、星域分布）——哪怕推演前百年根本到不了。
- 如果是类地球世界，就是完整地形、生物群系、资源分布、气候带。
- 原则：**世界存在的一切空间位置都建出来**。这是"最大原则"的空间向——资源、地理、邻接全部基于这个全景，后续推演不再补地图。
- **粒度**：初始是**粗但全面**的——每个空间位置存在（大陆/星域/区域轮廓），但细节概略。推演涉及具体子区域时，按"细化即锁定"规则在底座上细化（见本节核心规则 2）。

> 例：初始只说"西环大陆包含几个区域"；某一轮推演涉及其中一个子区域，根据合理性细化出地形/资源/聚落——**这一细化一旦确定即写入背景规则，后续不可再改**，除非继续细化。

#### ③ 初始实体（历史的起点，不是全貌）

这是推演的时间起点，不是全景——一个或多个初始实体卡片（种族 × 政权形态，见 §3.1）。实体会随推演增灭，但世界法则与空间全景不变。

#### ④ 用户干预（第四输入源，持续可注入，非初始化块）

前三块是一次性建立的**恒定底座**；用户干预是**推演过程中持续可注入**的第四输入源。用户可以在任意节点指定之后的某件事或某个历史走向（Decree，§3.7），但**不能回溯已发生历史**，且**必须经过历史法则与历史惯性判定**（§5.5）才能决定是否生效。这是用户从"旁观者"变为"可干预的神明/外部力量"，但仍受世界法则约束。

### 4.1 物理层（数值引擎，由法则派生）

物理层是**法则驱动的确定性数值引擎**，不依赖 LLM。它不再是"地理→资源→人口"的固定模板，而是**由世界法则全集派生的**一套关系模型：

- **法则决定"有哪些物理量"**：真实世界有粮食/人口/军力/航海；魔法世界有"魔力浓度/修炼进度/法阵覆盖"；真气世界有"气海/境界/天地灵气潮汐"。数值引擎能追踪的物理量，由该世界的法则声明（见 4.0 ①）。
- **物理量之间由法则决定联动**：真实世界是"人口×食物→马尔萨斯约束"；魔法世界可能是"魔力消耗→等价能量→施法上限"。
- 每个实体的结构化指标由这些规则驱动更新，**先算数值，再让 LLM 叙事化**。

> 数值引擎是"物理常识"，保证不出现"沙漠里凭空养出千万大军"这种自洽灾难。LLM 只在该硬约束的边界内发挥。但**"哪些物理量存在"由世界法则决定，不由我们预设**。

### 4.2 维度注册表（动态涌现，非预设）

**维度（发展轴）不是写死的，也不是用户预设的——是推演中涌现的。**

真实世界从 14 世纪到现代经历了大航海、文艺复兴、大革命、殖民、工业革命、两次大战、冷战、全球化——**完全不同的时代主题**，但它们全部运行在同一套法则下。严肃宏观推演最致命的地方是**它不是聚焦于某一个主题**：任何预先承诺主题的推演（写死"航海"轴、或让用户声明维度）都不是严肃宏观历史。主题是涌现输出，不是输入。

机制：

```
维度注册表（初始为空 或 最小集）
        │
        ├── agent 行为（开放式、符合法则的动作描述）
        │   + 数值引擎观察：哪些"轴"反复在塑造结果
        │
        ▼
   某轴反复出现（高频 × 后果权重 × 与法则一致）
        │
        ▼
   升格为结构化维度，开始用确定性数值追踪
   （例：某沿海实体反复投入航海 → "航海"轴浮现；
        某社会发展出修炼体系 → "修为"轴浮现）
        │
        ▼
   维度可消退（社会转向工业 → 航海轴淡出），新轴升起
   时代 = 注册表演化的轨迹，在事件日志中可见
```

**升格判定**（防失控）：agent 输出是开放的，但调度器按**"出现频率 × 后果权重 × 法则一致性"**把反复出现的领域晋升为受追踪维度。早期半开放，后期注册表稳定。既有自洽（维度一旦注册就用确定性数值一致地算）又有开放性（历史自己说出主题）。

**与 4.0 的关系**：世界法则/空间（确定性底座）固定且全面，维度（动态表示）在底座上自由涌现。底座全，涌现才不会因为缺底层而跑偏。

### 4.3 规则层（硬约束，LLM 校验）

世界特有的法则，表达为**规则字符串 + 触发条件**，复用现有 consistency check 管线（`constraint_check.rs` 关键词粗筛 + `consistency.rs` 语义复核）。例如：

```
规则: 起死回生不可能发生。
规则: 魔法的使用必须消耗等价能量。
规则: 任何跨越 <边境河> 的军事行动需要 2 tick 的集结期。
```

- **hard 级**：违反则事件打回重写或直接否决。
- 部分规则可带 `timeline_id` 作用域（复用现有 constraint 模型）。
- **规则来自 4.0 ① 的世界法则全集**——这里把法则落成可校验的约束。

### 4.4 叙事层（软约束，风格引导）

文明风格、价值观、禁忌。违反不阻断，但会在仲裁时被标记并在事件描述中体现张力。例如：

```
规则: 该文明崇尚荣誉决斗，背叛者会被社会排斥。
```

### 法则来源

1. **全景初始化（主路径）**：4.0 的全集——世界法则全集 + 空间全景 + 初始实体，用户/世界定义时一次性建立。
2. （进阶）从现有世界词条的 `constraints` 字段**导入**——把已有设定自动转成世界法则。

---

## 五、推演循环

### 总体流程

```
                    ┌─────────────────────────────────────┐
                    │          推演循环 (每 tick)            │
                    └─────────────────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        ▼                            ▼                            ▼
  0. 用户干预                   1. 同步广播                   2. 并行推演
  用户注入 Decree (§3.7)        上一tick的全局事件             每个活跃实体agent独立推演
   (可能作用于本tick)            按信息延迟+邻接分发给agent      读自己卡片+相关邻居+延迟广播
                                 + 已发生历史(唯一事实源)        + 已生效干预(外部力量/天命)
                                                                 新历史 = 已发生历史
                                                                 + 合理性 + 随机性 + 干预
                                                                 产出: 内部事件 + 状态增量
                                          │                          │
                                          ▼                          ▼
                                    3. 片尾仲裁                   4. 应用状态更新
                                    + Decree 判定 (§5.5)         数值层先算, LLM叙事化
                                    检测冲突/硬约束/历史锁定       更新实体卡片
                                    细化即锁定 + 历史即锁定
                                    实体消解 + 合并对账
                                          │
                                          ▼
                                    5. 动态调整agent池
                                    分裂→派生 / 吞并→合并
                                    灭亡→档案 / 复兴
                                          │
                                          ▼
                                    写 events/<tick>.json
                                    (全局时序日志, 追加不可改写)
```

### 5.1 同步广播（信息传播延迟）

**不是全知广播**。每个实体 agent 收到的"世界信息"是**延迟 + 局部**的：

- **邻接实体**：近实时（延迟 ≈ 1-2 tick）。
- **全球事件**：延迟 `infoDelay` tick，且只广播结构性事件（战争爆发、帝国崩溃、重大技术发现）。
- **远方**：只有"传闻级"摘要，可信度低。

这样古代世界"消息传得慢"的真实感成立，且防止所有 agent 同时全知反应（既省钱又自洽）。

**信息延迟 ≠ 不知道自己实体的历史。** 每个 agent 始终完整持有**自己实体及其核心关系的已锁定历史**（唯一事实源，§5.3）——信息延迟只影响"对外界新事件的知晓"，不影响"自己经历的已发生历史"。这保证：即使一个实体对外界消息滞后，它也绝不可能写出与自身已发生历史矛盾的内容（历史即锁定，§4.0 核心规则 3）。

实现：`events/<tick>.json` 是有序日志；agent 的输入装配时，按 `now - infoDelay` 截取可见窗口（外界事件），再按邻接关系过滤；**自身已锁定历史则始终完整注入**。

### 5.2 并行推演

调度器在**前端 lib 层**（TS）用并发 Promise 调度所有活跃 agent。每个 agent 一轮**非流式 single_chat**（复用 `api_proxy::single_chat`），**不启用工具循环**——MVP 追求可控与低成本。

**Agent 输入装配**（打进 system prompt + user message，无需工具）：

```
System:
- 你是 <实体名> 的执政者/历史推演器
- 世界模型全景摘要（世界法则全集 + 空间全景 + 维度注册表 + 规则/叙事，见 §4）
- 推演参数（当前 random/surprise/rigor 档位）
- 输出格式契约（严格 JSON，见 §6）

User:
- 我的实体卡片（当前完整状态）
- 邻接实体的关系 + 一句话动态
- 延迟窗口内的全局事件摘要
- 上次 tick 我的产出（internal.recent_events）
- 待解决的未决议题（active_issues）
- 已判定生效的用户干预指令（作为"外部力量/天命"的自然化输入, 见 §5.5）
```

**Agent 产出契约**（严格 JSON，调度器解析 + 校验 + 仲裁）：

```json
{
  "decisions": ["向邻邦要求贸易通道", "训练常备军"],
  "events": [
    {
      "type": "war",
      "target": "<邻邦id>",
      "description": "为争夺边境水源发动战争",
      "expected_outcome": "control"
    }
  ],
  "metric_delta": { "population": -2000, "military": 1500, "stability": -0.1 },
  "tech_delta": { "造船": 5, "冶金": 3 },
  "values_delta": { "探索欲": 2 },
  "notes": "内部王位继承问题仍未解决，支持度下降"
}
```

> 维度变化优先由**物理层数值引擎**计算（由世界法则派生 §4.1），agent 的 `tech_delta`/`values_delta` 只是**补充性信号**，最终以仲裁器合并结果为准。防止 LLM 拍脑袋涨维度——维度发展必须与投入/世界法则/文化因果自洽。维度名是动态的（这里以真实世界为示例），来自维度注册表（§4.2）。

### 5.3 片尾仲裁（自洽核心）

所有 agent 产出汇总后，仲裁器统一对账：

1. **冲突检测**：两个实体对同一事件的不同说法（如 A 宣称征服了 B，B 宣称击退了 A）→ 需要消解。
2. **硬约束检查**：全部事件过物理层 + 规则层。
3. **细化即锁定检查**：事件若涉及对既有区域的描述，必须**符合背景规则库**（§3.6）——只允许向下细化（新增更细层级），不允许在已确定层级上修改。违者打回或降级为"细化提案"。
4. **历史即锁定检查**：新事件必须**建立在已发生历史上**（唯一事实源 + 背景规则库中的历史事实）——不得与已确定的历史矛盾，不得改写/推翻已发生的事件。违反者打回或改写为与已发生历史一致的结果。
5. **消解策略**：
   - **等级仲裁**：数值模型优先（军力差决定战争结局）→ 其次全局仲裁 agent（一次性 LLM 裁决多个冲突，比逐对便宜）。
   - **合并对账**：被吞并实体的部分事件合并进吞并者视角。
   - **打回重写**：硬约束违反的单一事件，直接否决并生成一条"受阻"记录（保持历史连续），不重推。
   - **细化入库**：通过的细化写入背景规则库，`locked_tick` 标记，成为不可更改事实。
   - **历史锁定**：通过的事件写入唯一事实源，成为不可更改的已发生历史（新历史只能在其上叠加）。
6. **实体消解**（EvoSpark 启发）：同一实体被不同 agent 起了不同名字/状态 → 统一到 canonical id。

**仲裁器的"唯一事实源"原则**：每个 tick 结束时，`events/<tick>.json` + 更新后的实体卡片 = 该时刻世界的唯一事实。所有 agent 下一 tick 只读这个快照，不再各自持有旧真相。

### 5.4 动态调整 agent 池

实体数量无上限，随演化涌现：

| 触发 | 动作 |
|---|---|
| **分裂**（一个实体产出 secession 事件）| 派生 2 个子实体 agent：父卡片复制 + 拆分叙事 + 各得部分指标，`parent` 字段记录父 id |
| **吞并**（conquest 成功）| 被吞者卡片 `status: "extinct"` 移入 archive；吞并者吸收其人口/领土/部分指标；两个 agent 对同一领土的主张在仲裁时合并 |
| **灭亡**（collapse/灭绝）| 冻结进 `archive/`，不删——可被后人"考古"甚至"复兴" |
| **复兴**（进阶）| 后世 agent 发现 archive 中的文明遗迹 → 可触发文化/宗教复兴事件 |
| **分化**（进阶）| 同一实体内部出现文化/宗教分裂（如宗教改革）→ 可派生子实体，与原实体共享部分 identity |

**长尾聚合**：稳定的小部落/微邦数量大时，不逐个开 agent，而是**一个"区域层" agent 批量处理一簇**（`kind: "micro"`），只汇报结构性变化（谁灭了谁、谁并入谁）。大幅省 token。

### 5.5 用户干预判定（Decree Adjudication）

用户注入的干预指令（§3.7）**不直接生效**，必须经过判定，决定它以何种方式进入历史。判定发生在指令注入的 tick 的仲裁阶段，与 agent 产出走同一条管线。面向未来的指令（走向引导）与面向过去的指令（历史细化）走**同一套判定**，但面向过去多一道关卡。

**判定依据：**

1. **历史法则判定**——指令是否违反世界法则/物理层（§4.0①/§4.1/§4.3）？违反物理常识的不成立。
2. **历史惯性判定**——指令与已发生历史、当前趋势的吻合度（§4.0 核心规则 3 + §5.4）？与历史惯性剧烈冲突的会被扭曲。
3. **面向过去的额外关卡：与已发生未来兼容**——若指令是细化过去（`direction: "past"`），创造的过去人物/事件/细节**必须与已确定的历史及已发生的未来兼容**（§4.0 核心规则 3）。例如：不能在"已确定 tick 4 某文明未受外部影响而兴衰"的前提下，在 tick 3 创造一个"拯救了整个文明的天降神明"——那会改写未来。违者打回、调整或扭曲为与已发生历史一致的形式。

**判定结果谱系：**

| 判定 | 条件 | 效果 |
|---|---|---|
| **完全生效** | 符合法则 + 与历史惯性相容（past 还需与已发生未来兼容） | 指令作为"外部力量事件"注入后续推演（future）/ 写入过去（past），引导 agent 在法则内实现 |
| **打折生效** | 部分违背法则或惯性 | 以调整后形式生效（"X 国统一世界" → 一场大规模扩张战争，但因补给/邻国抵抗未竟全功） |
| **扭曲生效** | 与法则冲突但用户坚持 | 以悲剧/意外/变异形式实现（"复活死者" → 引发亡灵之乱，而非真复活） |
| **拒绝生效** | 完全违反法则（past 还包括与已发生未来矛盾） | 指令否决，但记录为"天意被阻"的历史事件 |

**自然化原则（关键）：** 用户指令在历史里被**自然化为世界内部的力量**——不是"用户说了一句"直接变成事件，而是包装成神谕、天启、强人崛起、思潮等，由 agent 在法则内实现。这样即使用户干预，历史依然自洽。用户干预与随机性的区别：随机性是**世界内部自发**的黑天鹅；用户干预是**世界外部确定性**的注入，但两者都过法则判定。

**不能回溯，但可细化：**
- **面向未来**（future）：指令只影响注入 tick 及之后的历史，不能改写已发生的过去。
- **面向过去**（past）：指令是在已发生的历史上**向下细化**（创造过去的人物/事件/细节），不改变已确定的宏观轨迹，且必须与已发生的未来兼容。**细化一旦判定通过即锁定**（写入背景规则库，§3.6），不可再改。

判定结果回填到 Decree 的 `verdict`/`verdict_note`，并在事件日志中可见（让用户看到"我的指令被如何实现/折中/拒绝"）。

---

## 六、实体 Agent 设计

### 注意力分层（成本控制核心）

**没有硬性数量上限，但推理资源不平均分配。** 每个实体在每 tick 被评一个活跃度：

```
active_score = f(内部动荡, 冲突强度, 技术/制度变化率, 与热点邻接)
```

按分数分档，决定推理深度与 token 预算：

| 档位 | 适用 | 处理方式 | 相对成本 |
|---|---|---|---|
| **hotspot** | 战乱/政变/分裂中 | 完整推演，深度叙事，大预算 | 4x |
| **regular** | 稳定发展 | 例行处理，紧凑增量 | 1x |
| **longtail** | 大量稳定微邦 | 批量聚合（区域层 agent），只报结构性变化 | 0.2x |

太平盛世便宜，动荡时期昂贵——**token 消耗跟随历史自身**。

### 宏观优先（不追踪个人级要素）

推演的是"国家与文明"，不是个人。**统治者、英雄、个人野心等个人级要素不作为独立机制追踪**——它们属于"村庄日常"级别的微观细节，违背宏观优先原则。政体/历史的变迁（包括领导层更迭、秩序重塑）由 agent 在叙事中自然涌现（如政体演化关口时 agent 描述"新秩序建立"），不设专门的个人机制。

---

## 七、随机性与严肃性（参数语义）

### 随机性（randomness）

- 每 tick 每个实体按 `randomness` 概率触发**黑天鹅事件发生器**。
- 生成候选小概率事件（天灾、天才诞生、意外发现、疾病、异象）。
- 触发后进仲裁器，与其他事件一起过自洽检查。
- `surprise`（意外结果比例）单独控制**计划外结局**：如"战争本应胜利，却因一场风暴失败"——发生在 `surprise` 控制的概率下。

### 随机性注入实体差异性

历史推演的差异不仅有决定性来源（地理→技术潜力→理念→选择），还有**随机性注入的差异**——小概率事件在特定实体上集中爆发，制造"为什么是它"的偶然性：

- **个体天才/奇观**：某个实体在 `randomness` 概率下诞生关键人物或重大发现，其**已注册维度**相应跳变（如一个天才航海家让沿海部落首次远航——"航海"若已注册为维度则跳变；魔法世界则可能是"一位天生圣体者让宗门修为跃迁"）。可遇不可求，由 randomness 注入，**扰动**倾向的执行而非决定倾向。
- **地理决定倾向，随机决定执行**：地理-文化设定了"什么方向有潜力"（确定性），而"某个具体时刻是否冒出天才/灾难/契机把它兑现"由 randomness 决定。
- **非平等发展**：实体间技术/理念天然不均衡。同一 tick，沿海实体可能已进入航海时代，内陆还在部落制。**默认状态就是不平等**——推演引擎不追求均衡发展，只追求每个实体的发展因果自洽。
- **对结果的影响**：randomness 提高 → 天才/奇观更密集 → 历史更跳跃、更"传奇"；randomness 降低 → 发展主要由地理-文化决定 → 更"必然"、更可预测。

### 严肃性 / 因果严密性（rigor）

- 高 rigor：拒绝巧合救场。若黑天鹅事件被用来"逆转"一个有明确因果的进程（如大军压境被一场龙卷风全灭），仲裁器倾向否决或改写为合理结果。
- 低 rigor：允许戏剧化巧合，事件更"传奇"。
- 与 randomness 的关系：两者正交。randomness 控制"事发生的概率"，rigor 控制"事如何被允许发生"。

### 档位预设

UI 提供快捷档位：`传奇`（high randomness, low rigor）/ `平衡` / `史学`（low randomness, high rigor）。也可手动滑杆微调。

---

## 八、自洽保证（汇总）

| 防线 | 机制 | 位置 |
|---|---|---|
| 1. 物理层数值引擎 | 世界法则派生的物理量确定性联动 | 调度器数值引擎（§4.1） |
| 2. 规则层硬约束 | 世界法则全集 + 复用 consistency 管线 | 片尾仲裁（§5.3） |
| 2.5 维度注册表 | 已注册维度用确定性数值一致追踪 | 调度器（§4.2） |
| 2.75 背景规则库 | 空间向细化即锁定 + 时间向历史即锁定 + 细化过去锁定, 已确定事实不可回溯修改 | 仲裁器（§5.3） |
| 2.9 历史即锁定 | 宏观轨迹锁定不可改写; 但允许在已发生时代向下细化过去 | 唯一事实源 + 仲裁器（§5.3/§4.0） |
| 2.95 用户干预判定 | future 过历史法则+惯性; past 再加"与已发生未来兼容"关卡才生效 | 仲裁器（§5.5） |
| 3. 片尾对账 | 唯一事实源 + 冲突消解 | 仲裁器（§5.3） |
| 4. 确定性 ID | canonical id + 事件时序 | 数据模型（§3） |
| 5. 信息局部性 | 延迟广播防全知矛盾 | 同步广播（§5.1） |
| 6. 结构化输出契约 | 严格 JSON schema 约束 agent | Agent 通信（§6） |

---

## 九、参数面板（UI）

推演控制台提供：

- **全景初始化**（推演入口）：世界法则全集 + 空间全景（按真实尺度铺全 §4.0②）+ 初始实体卡片集合（种族 × 政权形态正交），或从现有世界词条导入。
- **播放控制**：开始 / 暂停 / 步进 / 加速（多 tick 批量）/ 重置。
- **参数**：randomness / surprise / rigor 滑杆 + 粒度 + 时间片基准 + 时代跳跃开关。
- **预算监控**：本 tick 已用 token / 总预算，实时显示（含成本警告）。
- **实体面板**：当前活跃实体列表（数量、名称、活跃档位），点击查看卡片。
- **事件流**：每 tick 全局事件日志（可展开叙事、追踪因果链）。
- **干预注入**（Decree）：二选一——**面向未来**（指定之后的某件事/走向）+ **面向过去**（在已发生的时代细化，创造人物/事件/细节）。均提交后进入判定（§5.5），结果回填显示"完全生效/打折/扭曲/拒绝"。
- **反事实**（Phase 2）：一键存档当前状态 → 改参数 → 另起分支 → 对比两条历史。

---

## 十、桥接：提交到正式世界

（默认关，用户手动触发，批量选择事件）

1. 用户选中某 tick 区间的事件。
2. 点击"提交到正式时间线"。
3. 调度器把 SimulationEvent 转为现有 `Event` 写入 `timelines/`，自动触发 `event_cascade`：
   - 关系图新增 `参与事件` 边
   - 词条 `timeline_summary[]` 重算
   - 大纲章节联动（如有）
4. 可把关键实体卡片转成词条（`organization` 或 `location` 类型），形成正式设定。

这样推演结果可以"转正"，但**默认探索性分支不污染正式设定**。

---

## 十一、实施阶段

### Phase 0 — 数据模型 + 数值引擎（不含 LLM）

- `simulation/` 目录结构与 JSON schema（含 `lore.json` 背景规则库）。
- 物理层数值引擎（由世界法则派生的物理量联动，如地形→资源→人口→军力；含维度潜力上限 + 理念转化，维度名来自注册表）。
- 参数解析、黑天鹅概率判定（伪随机）。
- **初始世界状态编辑器**：定义世界法则全集 + 空间全景（§4.0）+ 初始实体卡片集合（种族×政权形态正交）。
- **背景规则库 + 细化即锁定**（§3.6/§4.0）：细化写入、层级锁定、禁止回溯修改的确定性逻辑（无 LLM，可单测）。
- 无 LLM，纯确定性，可单测。

**验收**：手写几个实体卡片（真实世界 / 魔法 / 真气各一例），跑 10 tick 数值推演，验证法则派生的物理量联动不失控，已注册维度实际值不超过潜力上限，且背景规则库的细化锁定逻辑正确（锁定后无法回溯修改、只能继续细化）。

### Phase 1 — 单 agent 推演 + 仲裁骨架（MVP）

- 调度器 + 单实体 agent（single_chat + JSON 契约）。
- 片尾仲裁：硬约束检查 + 细化即锁定检查 + 冲突消解骨架 + 唯一事实源落盘。
- **用户干预判定**（Decree, §5.5）：先做面向未来（future）——历史法则 + 历史惯性两道关卡, 判定结果谱系（完全/打折/扭曲/拒绝）；面向过去的细化在 Phase 2 落地。
- 注意力分层（先做 hotspot/regular 两档）。
- 播放控制 UI + 事件流展示 + 干预注入入口。

**验收**：一个 5-8 实体的初始世界，跑 20 tick，验证：无硬约束违反、事件时序连续、实体卡片状态合理演化；注入干预指令后，判定符合法则且不回溯历史，结果回填正确。

### Phase 2 — 动态 agent 池 + 多 agent 交互

- 分裂 / 吞并 / 灭亡 / 档案全流程。
- 多实体并行推演 + 跨实体冲突仲裁（等级仲裁 + 全局仲裁 agent）。
- 信息传播延迟。
- 长尾批量聚合。
- 反事实分叉。
- **面向过去的细化判定**（Decree `direction: "past"`, §5.5）：在已发生时代创造人物/事件/细节，判定含"与已发生未来兼容"关卡。

**验收**：实体数随演化增减，吞并/分裂在事件流中连续自洽，无"两实体同时声称同一块地"的未消解矛盾；面向过去的细化正确锁定，且不会与已发生的未来产生矛盾。

### Phase 3 — 深化与打磨

- 文明复兴的深化。
- 从现有词条导入世界法则。
- 桥接到正式世界（提交到 timelines/）。
- 预算优化（上下文压缩、摘要装配复用现有管线）。

---

## 十二、风险与开放问题

| 风险 | 缓解 |
|---|---|
| **token 成本失控** | 注意力分层 + 长尾聚合 + 非流式 + 预算硬上限；宏观粒度（事件只记结构性变化） |
| **LLM 背真实历史模板** | 架空世界天然匿名；世界法则越独特越难套模板；仲裁器可检测"事件与输入世界设定无因果" |
| **跨实体矛盾累积** | 片尾对账唯一事实源 + 实体消解；agent 只读快照不再各自持旧真相 |
| **数值层与叙事层脱节** | 数值先行、叙事后写；仲裁时叙事必须落在数值边界内 |
| **实体数量爆炸** | 长尾批量聚合 + 活跃度自然淘汰 + 吞并合并；无硬上限但成本随活跃度自平衡 |
| **推演无趣/过于平滑** | 黑天鹅发生器 + 随机性档位；autojump 跳过平顺期、放慢动荡期 |
| **维度涌现失控** | 升格判定门槛（频率×后果×法则一致性）+ 注册表可人工调整/冻结；维度生命周期（register→retire）纳入事件日志 |
| **全景初始化成本** | 一次性成本，可接受；支持从现有世界词条/已建地图导入降低录入成本；空间全景按需加载（不是每 tick 全量读） |
| **已锁定事实被后续违反** | 细化即锁定（§3.6/§5.3）：仲裁时事件必须符合背景规则库, 只许向下细化不许回溯修改 |
| **用户干预破坏自洽** | 干预必须过历史法则 + 历史惯性两道关卡（§5.5）才能生效; 面向未来不回溯历史, 面向过去只许细化且必须与已发生未来兼容; 不能凭空造物或改写过去; 判定结果透明回填给用户 |

**开放问题（实现时再定）**：

1. 全局仲裁 agent 的调用频率与合并策略（逐 tick 一次 vs 仅冲突时触发）。
2. 数值引擎的复杂度边界——多复杂才够"物理感"而不喧宾夺主。
3. 实体卡片迁移到正式词条时，关系图映射规则（`organization`/`location` 类型 + `located_in`/`borders`/`capital_of` 关系）。
4. 用户干预判定的阈值标定——"与历史惯性冲突到什么程度"算打折/扭曲/拒绝。
5. 干预强度三档（command/lean/nudge）对 agent 行为的引导权重如何设定。
4. 维度升格的具体判定公式与阈值（Phase 1 观察后标定）。
5. 空间全景的表示粒度——可观测宇宙级地图如何结构化（网格/分层/惰性展开），才能既"全"又不拖垮推演。

---

## 附：借鉴来源

- **WarAgent**（AI 研究院）— 多国家 agent 推演国际冲突；Board/Stick 分离对外关系与内部状态；秘书 agent 校验行动合法性；匿名化防 LLM 背史。**验证了多实体并行推演可行性，以及"战争总会爆发"的涌现性。**
- **EvoSpark**（ACL 2026）— 长时程多 agent 叙事一致性；指出"社会记忆堆叠"与"叙事-空间错位"两大失败模式；Director agent 持续实体消解；把幻觉反转为"本体升格"的创造性驱动。
- **Oikoumene / Simulating Prehistoric and Ancient Worlds** — 传统 ABM 世界史模拟；地理→资源→人口→制度涌现的确定性因果链，作为物理层的范式参考。
- **Project Psychohistory** — 阿西莫夫式单 agent 文明演化；"Seldon Crises + Mule 事件"即随机性注入的文学化表达。
- **Generative Agents（Park et al. 2023）** — 观察/规划/反思三组件，作为实体 agent 状态维护的参考。
